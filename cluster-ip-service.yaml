apiVersion: v1
kind: Service
metadata:
  name: clusterip-service
spec:
  type: ClusterIP
  # Uncomment the below line to create a Headless Service
  # clusterIP: None
  selector:
    app: django
    department: production
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080